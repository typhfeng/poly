{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Load talk history",
      "type": "shell",
      "command": "${env:HOME}/.local/bin/talk_history.sh",
      "args": ["${workspaceFolder}"],
      "runOptions": {
        "runOn": "folderOpen"
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      }
    }
  ]
}
